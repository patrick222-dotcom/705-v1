# State-of-the-world brief — 2026-09-02

Companion to `session-2026-09-02-domain-and-naming.md` (which records *decisions*). This file
records *state*: what the repo, docs, infrastructure and app actually contain right now.

**How it was produced:** six independent survey agents (git history, branding scope, docs
accuracy, backlog hygiene, infrastructure, app health), each finding then re-checked by a
separate adversarial verifier that reopened every cited file and line. Verdicts are
CONFIRMED / CORRECTED / REFUTED; corrected facts are stated as corrected, and disagreements
are preserved rather than smoothed over — see the final section. 13 agents, ~1.2M tokens.

**Spot-checked by hand afterwards** (all four confirmed): the stale local deploy ref, the
`CNAME` deletion in the iCal stack, the duplicated `scrubpayErrors` literal, and
`nursingWagePlannerData` containing no brand string.

> Findings are evidence-backed but not infallible. Re-verify anything before acting
> destructively on it.

---

## State of the World — ScrubPay / BadgeBudget (`/home/user/705-v1`, HEAD `b17d86e`)

### Landmines — read before touching anything

| # | Landmine | Evidence |
|---|---|---|
| 1 | **`nursingWagePlannerData` is the user-data localStorage key and must never be renamed.** It contains no brand string, so a "ScrubPay → BadgeBudget" find/replace misses it — and renaming it orphans every anonymous user's data. 14 executable sites + 1 comment. | `index.html:519` (`STORE_KEY`), :1722, :1731, :1760(comment), :1765, :1781, :1820, :2418; `BACKUP_KEY = STORE_KEY + '::' + uid` at :1580, used :1585, :1592, :1758, :1779, :1798, :2415 |
| 2 | **Four brand-bearing storage keys are data, not branding.** Renaming `scrubpay_anon_id` mints a fresh UUID per device and permanently severs historical `events.anon_id` joins; the others silently drop queued feedback / pending invites. | `index.html:964` (`ANON_ID_KEY`, read :967, written :968), :943 (`FEEDBACK_PENDING_KEY`, used :1392/:1394/:1888/:1896), :1032 (`SWAP_INVITE_KEY`, TTL :1033, used :1055/:1058/:1062/:1065), :398 (`ERR_KEY='scrubpayErrors'`) |
| 3 | **`'scrubpayErrors'` is hardcoded a second time** at `index.html:466` inside `window.__copyErrorLog` and does *not* use `ERR_KEY` (grep for `ERR_KEY` returns only :398, :402, :408). Rename one without the other and you break both the boot-failure diagnostic screen **and** Settings → "Copy error log". | `index.html:398`, `:466` |
| 4 | **`'scrubpay-swaps'` is a live md5 salt in the deployed DB**, not a string. It derives `poster_key`, the pseudonymous identity the whole swap board's anonymity model rests on. Verified present in the *live* `public.swap_board` function (salt at char offset 761), and it is the only deployed function containing the brand. | `supabase/migrations/001_swap_board.sql:158`; live Supabase MCP query on `public.swap_board` |
| 5 | **`@scrubpay` is the .ics self-recognition sentinel.** Export stamps it; import drops any UID containing it. Change the scheme and every previously-exported event re-imports as a duplicate. Users never see it. | `index.html:2250`, `:2271` (export), `:690` (`if(uidRaw.indexOf('@scrubpay')>=0){ droppedOwn++; return; }`) |
| 6 | **The repo has no `main` branch.** `claude/migrate-to-github-deploy-3F5RD` is the de facto default and is deliberately in the workflow triggers. Add `main` to the trigger *before* removing the claude branch, never in the same commit. | `.github/workflows/deploy.yml:5-10` (+ in-file warning at :8-9); `git branch -a` → no main/master anywhere |
| 7 | **The publish step is a hardcoded 3-file `cp`.** Anything new silently 404s. Deleting `cp CNAME _site/` clears the custom-domain setting and knocks the site off badgebudget.com. | `.github/workflows/deploy.yml:36-42` (`cp index.html`, `cp pdf.worker.min.js`, `cp CNAME`, warning comment :40-41); `CNAME` = `badgebudget.com`, single commit `46ee4a8` |
| 8 | **The local `claude/migrate-to-github-deploy-3F5RD` ref in this checkout is 14 commits BEHIND origin** (local `d101a88`, origin `46ee4a8`), with a 321-line `index.html` delta (257 ins / 64 del). A build run that checks it out without fetching, then merges or force-pushes, reverts production. The clone is also **shallow** (`.git/shallow` present), capping history claims. | `git rev-list --count local..origin` → 14; `git diff --stat local origin -- index.html` → 321 |
| 9 | **Boot hardening is gate-protected and must not be weakened:** 8s watchdog, Supabase null-guard, 4s `getSession` race. All three verified present at the stated values. | `index.html:441`/`:461` (setTimeout … `}, 8000);`), `:500-506` (null-guard; **23** downstream `if(!supabase)` guards, not 4), `:513-516` + `:1685` (`withTimeout(getSession(), 4000)`) |
| 10 | **SRI on all 5 CDN scripts is gate-protected.** `grep -c integrity=` → 5, all `sha384` + `crossorigin`, all pinned. The only same-origin runtime file is `pdf.worker.min.js`, loaded relatively from JS. | `index.html:14-18`, `:2428` (`workerSrc='pdf.worker.min.js'`) |
| 11 | **Do NOT delete `claude/ical-subscription-sync`.** An earlier survey listed it as stale; that was **CORRECTED** — it is the base branch of open draft PR #50 and carries the whole unmerged iCal feature, and PR #57 is stacked on it. | `git log ical-subscription-sync ^deploy` → `bb3a205` absent from deploy |
| 12 | **`claude/scrubpay-domain-purchase-j9cqf6` is the head of two PRs** — #58 (already merged at `4d8ee1e` → `46ee4a8`) and #60 (open, `b17d86e`). Don't delete it after merging #60 without checking. | GitHub API head refs for #58/#60 |

---

### Current state by area

#### Repo / git

- **Deploy branch, last 7 days:** 9 single-parent squash commits on `origin/claude/migrate-to-github-deploy-3F5RD`, 2026-08-26 → 2026-09-02. Eight nightly builds at 04:1x ET off `claude/clause-md-review-9tqlj8` (`e61ad0c` #49, `ff159aa` #51, `b83105f` #52, `546c762` #53, `2ddabcb` #54, `757e034` #55, `0722448` #56, `bb80d77` #59), plus one interactive infra commit `46ee4a8` (#58, CNAME → badgebudget.com) at 10:36 ET.
- All 8 nightly diffs were read line-by-line: **none** touch `integrity=`, the watchdog, the `getSession` race, or any wage function. That is verified, not merely asserted by commit message. Scope was 1–9 changed `index.html` lines each (copy, hints, one `aria-label`, one PTO `NumInput` wrapped in a div).
- **Commit-trailer anomaly:** seven consecutive nightly commits (`ff159aa` → `bb80d77`) carry the byte-identical trailer `Claude-Session: https://claude.ai/code/session_01GGWUJsjTww44sqB58U5Ahp`. `e61ad0c` and all seven earlier nightlies carry none. Nothing in tracked source sets it (`git grep 'Claude-Session'` → 0 hits), and genuinely different sessions do emit different values (`056bb4a` → `session_011yUZhta1pLEcvQZbxVgtaB`). Most likely: the URL is **pinned in the nightly Routine's prompt**, so no nightly run is traceable to its own session. Not proven — check the Routine.
- **Open work:** the iCal auto-sync stack is the big one — PR #50 (draft, base `claude/ical-subscription-sync`, `bb3a205`) and PR #57 (draft, `claude/ical-branch-progress-4x2baj` @ `617704d`, stacking `1f75ca8`/`056bb4a`/`617704d`). Three-dot diff vs deploy: 10 files, 653 insertions, `index.html` 428. Adds `supabase/functions/ical-proxy/index.ts` (+103) and `supabase/migrations/002_ical_subscription.sql` (+35), neither of which exists on deploy.
- **Merge conflict on the iCal stack is certain, not likely.** Two-dot diff vs deploy: `BACKLOG.md` 150 changed lines, `CNAME` 1 deletion, `deploy.yml` 3 deletions. `bb3a205` predates 7 nightly BACKLOG rewrites plus the CNAME commit. Rebase, take the deploy side for the Done log, and explicitly confirm `cp CNAME _site/` survives.
- **PR #46** (`claude/share-link-swap-board-hh9jsc`, `7950753`): one commit, `index.html` only, +10 lines in `AuthModal`. **Corrected:** head commit is dated 2026-08-24 12:58:44Z (not "untouched since 08-23"); only 5 of the 10 lines are visible — the rest is a JSX comment noting the real fix is a paid Supabase custom-auth-domain add-on; the full copy is *"Google will ask you to continue to supabase.co — that's the secure sign-in service ScrubPay uses. Your password is never shared with us."*; and the title's "+ backlog updates" already landed via #45. Open 10 days.
- **PR #60** (this session's docs): **corrected** — head is `b17d86e`, two commits, 3 files / 270 insertions including a new 193-line `docs/session-2026-09-02-domain-and-naming.md`. Docs-only.
- **Stale branches — corrected set is FOUR, not six:** `claude/clause-md-review-9tqlj8` (nightly working branch — leave it), `claude/reddit-agent-personas-threads-q3mobt` (→ #42/`d101a88`), `claude/reddit-seed-phase1` (→ #30/`dd513e9`), `docs/reddit-persona-pipeline` (its own PR #29 was **closed unmerged**; the doc landed via #41/`9296e6f`), plus `crawler-pushtest` (`293196d`, a one-line `.crawler-heartbeat` auth probe from 2026-08-11, safe to delete). The earlier "two-dot diffs show only deploy being ahead" evidence is only true for `clause-md-review-9tqlj8`.
- **Working tree is clean.** `docs/session-2026-09-02-domain-and-naming.md` is **tracked and committed** as `b17d86e` — an earlier survey claimed it was untracked and needed urgent committing; that was **REFUTED**. Drop that action item.

#### The app file (`index.html`)

- 4,394 lines / 269,246 bytes. `<style>` :19-383; boot IIFE :389-481; Babel block :483-4393; mount at :4392. Longest line is 824 chars (the `Ic.gear` SVG at :488).
- **God components:** `App` :1526-2705 (1,180 lines, **38** `useState` in :1527-1570) and `SwapsSheet` :3445-4266 (822 lines, **32** `useState` in :3446-3492) — both counts higher than earlier reported. Together ~45% of the file. File-wide: 101 `useState`, 16 `useEffect`, 13 `useMemo`, 15 `useRef`.
- **Zero memoization on the calendar path:** `React.memo` 0, `useCallback` 0. **Important correction:** stabilizing `keyOf` alone will NOT enable `React.memo` on `MonthSection` — `statOf` (:1926), `onPick` (inline ternary arrow, :2611) and `secRef` (inline arrow, :2614) are equally fresh every render. Do not ship "stabilize keyOf" as a perf fix; it is a no-op.
- **Duplicated date helpers (real):** module-level `parseISODate`/`keyOfDate` at :525-526 (comment at :524 says they're "shared by App + the month-grid components") are redeclared verbatim inside `App` as `parseISO`/`keyOf` at :1905-1906. Bodies are character-identical. Plus 6 inline `split('-').map(Number)` repeats at :741, :765, :1902, :2121, :3039, :3048. Both module-level helpers are live (:530/:532/:536). Ship as dedup only, no perf claim.
- **Clean:** zero TODO/FIXME/HACK/XXX, zero `console.log(`, zero dead top-level code (91 top-level ids, minimum reference count 2, independently re-derived — earlier "34 top-level functions" was a mislabel; the real counts are 53 `function` + 38 `const` + 1 `class`, and 34 is the size of the refs==2 bucket).
- **22 empty/comment-only catch blocks across 61 catch sites** (denominator corrected from 63) — nearly all deliberate localStorage-quota guards with intent comments (:409, :1848). One sync `try/catch` wraps an async `.then` at :1779 and cannot catch its rejection; not a live bug, because `saveToSupabase` (:919-939) resolves unconditionally.
- **CSP** at :6 covers all 7 runtime hosts with no mismatch. Missing `base-uri`/`object-src`/`form-action` — low value here since `script-src` already allows `'unsafe-inline'`. **Note:** `frame-ancestors` *cannot* be added via a `<meta>` CSP (it is ignored there) and GitHub Pages sends no `X-Frame-Options`, so clickjacking protection is simply unavailable on this hosting.
- **Two independent 15s polls:** App sync (:1855, cleanup :1856) and SwapsSheet (:3552, cleanup :3553, plus a `visibilityState` guard at :3549 that App's poll lacks). Both correctly torn down.
- **Branding tally (corrected):** 21 `scrubpay` occurrences on 18 lines in `index.html`, of which **8** are user-visible (`:7` title, `:386` splash wordmark, `:456` boot-failure heading, `:2283` .ics PRODID + CALNAME, `:2510` header wordmark, `:3627`/`:3630` share text, `:4278` onboarding wordmark). Outside `index.html`: **49 occurrences across 17 files**. `badgebudget` appears in only 4 files (CNAME, deploy.yml comment, CLAUDE.md, the session doc) and **zero times in shipped code** — the rename is greenfield, no half-applied state.
- **Export filenames are safe to rename:** `importData` (:2293-2304) does `JSON.parse` on the FileReader text and never sniffs the filename; `accept=` attrs are picker filters only.

#### Docs accuracy (`CLAUDE.md`)

Mostly accurate — SRI count, pinned versions, 8000/4000/512KB/15000, the 6-table/8-function migration, both 2026-07-30 RLS fixes, and the reveal_match `42702` alias fix (`001_swap_board.sql:331`) all verify exactly. Defects:

- **Analytics list is stale by 21 names.** `CLAUDE.md:76-82` documents 7 event names; `index.html` has 29 `track()` call sites yielding **28 distinct names** (e.g. `day_event_added` :2159, `ics_exported` :2291, `swap_group_created` :3583, `swap_plan_applied` :3922).
- **Undisclosed collection, on two paths.** `track()` sends `page` (`index.html:982`) and `user_agent` (`:983`, 400 chars). **Missed by earlier surveys:** `submitFeedback()` sends the *same two fields* at `:952-953` — on the one surface carrying users' own words. Neither `CLAUDE.md`'s feedback bullet nor its owner query mentions either column. `grep -i user_agent CLAUDE.md docs/*.md` → 0 hits. The "never PII" framing is undercut; a full UA string is fingerprinting-grade.
- **"'not set up yet' UI state is retired" is false.** `tablesMissing` has one hook (`:3448`), **six** setters (:3504, :3506, :3511, :3533, :3537, :3566), a dedicated detector `isMissingSwapTable()` (:1009-1013), the 🛠️ screen (:3968-3974), and **six sibling render branches gated on `!tablesMissing`** (:3976, :3990, :4000, :4009, :4028, :4212). Reword to "defensive fallback, unreachable in normal operation" — deleting it is not doc-sized work.
- **Self-contradiction on the publish set.** `CLAUDE.md:165` says Pages publishes only `index.html` + `pdf.worker.min.js`; `CLAUDE.md:93-96` correctly says three files including the load-bearing CNAME. :165 is a live foot-gun.
- **Two undocumented localStorage keys:** `scrubpayErrors` (:398) and `nursingWagePlannerData::<uid>` (:1580).
- **`design-system/` is entirely undocumented** — 12 files, never mentioned in a CLAUDE.md whose Architecture section opens "everything lives in index.html". Next agent can't tell if it's canonical or stale. (Correctly not deployed.)
- **Cited test harnesses do not exist in git.** `te_swap_p2_algo.js` (the "27-assertion suite") and `scratchpad/rls_audit.js` (the "29/29 audit") were never committed on **any** branch — verified by `git log --all --diff-filter=A` and by enumerating `git ls-tree -r` across all 12 refs. The only tracked test is `scripts/test_groom_seed.mjs`. Every "29/29 + 5/5" claim is unreproducible from the repo.
- **`SUPABASE_ACCESS_TOKEN` is verifiably uppercase in this container** (`env` shows only the uppercase name). The CLAUDE.md "Fixed 2026-09-02" note is true — an earlier survey marked this unverifiable. Don't re-open it.
- **Verified accurate and worth not re-checking:** single-file/no-build (no `package.json`); 5 CDN scripts with exact versions; local pdf.js worker + `isEvalSupported:false` (:2427-2429); 9 Supabase tables used by the client (:930, :948, :977, :1074, :1099, :1130, :1161, :1306, :1354); the 15s sync design (:1830-1857) and that no Realtime exists (CSP has no `wss:`); invite-link mechanics (:1031-1053); that the domain move needed zero code changes (`grep -c github.io index.html` → 0; OAuth `redirectTo` is `origin + pathname` at :3319).

#### Backlog shape (`BACKLOG.md`, 669 lines)

- **The Queue contains no runnable work, and has not for ten consecutive nights.** From 2026-08-23 through 2026-09-01 the Queue held exactly ONE open item — the un-runnable groom-dedupe defect (L51-58) — while ten builds shipped, every one sourced from a persona or Reddit candidate block. This is the exact pattern the 2026-08-22 "Queue focus pass" (L421-440, diagnosis quoted at L425-431) was created to end. Reproducible by scanning `BACKLOG.md` at each of the last 14 commits, not inferred.
- The new P1 Savings-goals item (L26-48) entered only on 2026-09-02 (`46ee4a8`) so it hasn't been skipped yet — but it is undecided design (two directions at L33-38, a new persisted `goals` array at L39-40) and violates the file's own "single run can finish" rule (L62-66).
- **Only one ready-to-build open item exists, and it is not in the Queue:** L242-248, the paystub 0-differential-rows note. Its code citation `index.html:2762/2784` is still **accurate** (`{rows.length>0 && <>` at :2762, `</>}` at :2784, no else branch). Its title says "PTO day" but the surface is `PaystubReview` (:2709) — retitle when promoting.
- **Counts:** 32 open / 15 struck items; Done log is **31 dated entries** spanning L305-658 = **354 lines = 52.9%** of the file (earlier "27 entries" and "353 lines" both wrong).
- **Duplication:** all 15 struck persona lines mirror 14 Done-log entries; the "Keep ~X%" chip is struck twice (L212, L229-230) against one Done entry (L399). **Corrected:** the parked swap-handoff item absorbs only **two** seed themes (L291, L297), not four — L299 (`swap-eligibility-hidden-rules`) is genuinely uncovered, because the item's OT/hours-rule flag serves the *approver*, not the nurse pre-post. And **three** candidates map to `paycheck-projection` (L282, L283, L288) from three distinct seed ids, not two.
- **Already-shipped but still open as candidates:** L287 (OT×differentials — shipped 2026-08-04 L649, plus L592/L410/L389; 12 `isOvertime` occurrences in index.html), L283 (take-home-after-taxes — is the product; inputs at index.html:2755/2757/2759), L286/L294/L295 (partially shipped; only the "floated" tag and a requested-vs-approved PTO state remain uncovered).
- **Root cause of all four "already-done but still open" items is one bug, not four slips:** `scripts/groom_seed.mjs:178` builds its coverage corpus as `CLAUDE.md + stripManagedBlock(BACKLOG.md)` and tests keyword *presence* (threshold 2 distinct ≥4-char hits, `:56`). So the file's own closing rule (L668-669, "don't re-add anything already in Done or Blocked") is unenforceable, and **archiving the Done log would resurrect every shipped theme as a candidate**. Build the shipped-ids list first, archive second.
- **`harness:` tags are inconsistent:** L27 uses `harness: drivable` (space), L243 uses `harness:drivable` (none), L51 and 8 of 9 parked items are untagged. The machine-written block (L275-303) is the only correctly tagged and correctly drivable-first-ordered section.
- **Stale code refs, corrected:** L223 → `index.html:4019`; L226 → **`index.html:4129-4130`** ("SUGGESTED FOR YOU"), **not** :3945, which is the signed-out gate on a different screen. Both drifted +193/+196 from `824547e` (2026-08-21), where the cited numbers were exact.
- **Environment notes (L263-273), corrected:** the "no Management API from scheduled runs" bullet is **provably false** — six Done entries record "GROOM (Supabase MCP live)" (L317, 326, 345, 354, 363, 374). The "cannot git push" bullet is only *stale-looking*; git cannot distinguish scheduled from interactive pushes, so it is unproven either way. PR range is **#34-#59 over ~17 nights**, not #43-#59 over ~14.
- **Cross-file duplication:** `BACKLOG.md:259-261` ↔ `CLAUDE.md:174-181` (Resend blocker). Larger one missed earlier: **`CLAUDE.md:291-295`** duplicates four parked items (BACKLOG L157, L161, L165, L168) **and still lists two items as deferred that shipped weeks ago** — OT×differential stacking (2026-08-04, L649-656) and FAB overlap (2026-08-09, L643-648).
- **Dead path ref:** `BACKLOG.md:74` cites `scratchpad/rls_audit.js`, which doesn't exist; `CLAUDE.md` makes the same citation.
- **Old domain in forward-looking text:** `BACKLOG.md:1` (title), `:130` ("a ScrubPay ingest endpoint", unbuilt design), `:468` (invite-link shape `…/705-v1/?join=ABC123`). Done-log history can stay.

#### Infrastructure

- **One workflow, ever.** `deploy.yml` is the only file under `.github` on every one of the 12 refs; one add commit (`9a56bc3`), zero deletes. Permissions `contents:read`/`pages:write`/`id-token:write` (:13-16), concurrency `pages` no-cancel (:18-20), four actions tag-pinned not SHA-pinned (:30, :33, :45, :51).
- **Zero CI gating.** The only `run:` block in the workflow is the three `cp` lines. `index.html` is Babel-transformed in-browser, so a JSX syntax error is undetectable at deploy time and ships live. `scripts/test_groom_seed.mjs` passes **33/33** but nothing invokes it (repo-wide grep for its name hits only `CLAUDE.md:36`, `BACKLOG.md:609` — which still says "15/15" — and its own header).
- **No `.gitignore` anywhere, ever** — verified across all 12 refs and all history — combined with CLAUDE.md's "commit everything" instruction to an autonomous agent. The RLS audit is documented as minting real admin-API users, so a dumped token landing in the tree is a live risk.
- **Only 6 swap_* tables have a migration.** `user_data`, `feedback`, `events` — the tables the core product depends on — exist only in the live project. `grep -cE 'user_data|public\.feedback|public\.events' supabase/migrations/001_swap_board.sql` → 0. No `config.toml`, no CLI scaffolding, no drift detection. The migration itself is 344 lines: 6 tables, 6 RLS enables, 15 policies (each with a drop-if-exists), 8 security-definer functions, **3** indexes (:342-344 — corrected from 2).
- **Single Supabase project, no staging.** Ref `mnnlgcxnvodjwlhhiphq` hardcoded at `index.html:503` (+ preconnect :10, CSP :6) and in `.mcp.json`. Migrations, RLS changes and adversarial audits all run against production. The dev-DB backlog item sits under "Needs a dedicated session", **not** the Queue — the nightly loop will never pick it up. Free plan allows 2 active projects, so a dev project costs nothing.
- **Only tracked credential is the public anon JWT** (`index.html`, `role=anon`, ref matches, `exp` 2036-06-28) — correct to ship client-side. Independent scan for `service_role`/`sbp_`/`re_`/private keys matched only that plus two `.agents/skills/*.md` prose mentions with no values.
- **Nightly automation has zero repo representation** — no cron workflow, no `.claude/settings.json`, no hooks. `.claude/` holds only two committed mode-120000 symlinks into `.agents/skills/`, pinned by `skills-lock.json`. If the Routine is deleted, the loop stops with no repo-visible signal.
- **`.mcp.json` runs `@supabase/mcp-server-supabase@latest`** — unpinned.
- **README.md is actively wrong infra documentation.** Zero brand strings; claims "any push to `main` or `master` will automatically deploy" (:25, repeated :40) when neither branch exists; points at the generic github.io URL (:30); never mentions badgebudget.com, the real deploy branch, or the 3-file publish list; ends "No manual Netlify deployment needed!" (:44).
- **Google Fonts is an undocumented 4th external runtime vendor**, with no SRI — `fonts.googleapis.com` stylesheet (Bricolage Grotesque + Plus Jakarta Sans) pulling from `fonts.gstatic.com`, both whitelisted in the CSP at :6. Failure mode is graceful (fallback fonts), so lower severity than unpkg, but it is not covered by the "all 5 CDN scripts have SRI" assurance.
- **No favicon, no manifest, no OG/Twitter cards, no theme-color, no meta description.** `index.html`'s entire head is charset, viewport, CSP, title, 3 preconnects, 2 dns-prefetch. `/favicon.ico` 404s on badgebudget.com.
- **Branding surface outside the repo:** the GCP OAuth consent screen still shows `mnnlgcxnvodjwlhhiphq.supabase.co`. Setting the app name + logo there is free and is the most prominent branding a signing-in user sees; the Supabase custom auth domain that would fix the host string is a paid add-on (Pro $25/mo + $10/mo). No grep will find this. (`CLAUDE.md:132-136`)

---

### Prioritised cleanup — risk of breakage × cheapness

1. **Fetch before touching the deploy branch.** Add to `CLAUDE.md`'s nightly-loop step 2 (which currently only says "restarted from the deploy base"): `git fetch && git reset --hard origin/claude/migrate-to-github-deploy-3F5RD`. Prevents a 321-line production revert. *Evidence: local `d101a88` vs origin `46ee4a8`, 14 behind.*
2. **Fix `index.html:466` to use `ERR_KEY`.** One-line change, must land *before* any rename. Guards the boot diagnostic and Settings "Copy error log". *Evidence: `index.html:398` vs `:466`.*
3. **Strike the publish-set clause from `CLAUDE.md:165`.** It contradicts `:93-96` and would lead someone to drop `cp CNAME _site/`. One-line delete. *Evidence: `CLAUDE.md:165` vs `:93-96`, `deploy.yml:36-42`.*
4. **Strike the two shipped entries from `CLAUDE.md:291-295`** (OT×differential stacking → shipped `BACKLOG.md:649`; FAB overlap → shipped `BACKLOG.md:643`) and replace the remaining four with a pointer to `## Needs a dedicated session`. Prevents a groomer re-opening finished work. *Evidence: `CLAUDE.md:291-295` vs `BACKLOG.md:157/161/165/168` and `:643-648`/`:649-656`.*
5. **Add a `.gitignore`** covering `node_modules/`, `.env*`, `scratchpad/`, `*.log`, Playwright artifacts, `*token*`. Cheap; the audit mints real admin credentials. *Evidence: none has ever existed on any of 12 refs.*
6. **Promote `BACKLOG.md:242-248` into `## Queue` and retitle it** ("paystub review sheet", not "PTO day"). It is the only ready-to-build open item and its code refs at `index.html:2762/:2784` are still accurate. Ends the empty-Queue pattern for at least one night. *Evidence: `BACKLOG.md:242-248`, `:316`; `index.html:2709/:2762/:2784`.*
7. **Park the two Queue items that can't be finished in one run** — Savings-goals (`BACKLOG.md:26-48`, undecided design) and the groom-dedupe defect (`:51-58`, needs a shipped-ids list, and is tooling the iPhone harness can't reach) — each with a one-line reason, per the file's own rule at `:62-66`. Optionally split off the already-decided forward "% of goal" slice.
8. **Delete the four stale branches** — `claude/reddit-agent-personas-threads-q3mobt`, `claude/reddit-seed-phase1`, `docs/reddit-persona-pipeline`, `crawler-pushtest`. **Do NOT delete `claude/ical-subscription-sync`** (base of open PR #50, PR #57 stacked on it) or `claude/clause-md-review-9tqlj8` (nightly working branch). Check `claude/scrubpay-domain-purchase-j9cqf6` before deleting — head of two PRs.
9. **Delete the 15 struck persona lines and the duplicate "Keep ~X%" entries** from `BACKLOG.md` (L206, 207, 211, 212, 219, 220, 228, 229, 241, 249, 252-256, and L229-230). Pure subtraction; the Done log at L306-534 is the record.
10. **Refresh the two stale code refs:** `BACKLOG.md:223` → `index.html:4019`; `BACKLOG.md:226` → `index.html:4129-4130`. Convert both prose "Not sandbox-drivable" notes to `harness:needs-live-auth`, and normalise every open item to `harness:<tag>` (no space).
11. **Document `page` + `user_agent` on both the analytics and feedback bullets in `CLAUDE.md`** (and add them to both owner queries), or drop them from both inserts. Undisclosed collection on the feedback path is the sharper edge. *Evidence: `index.html:982-983` and `:952-953`; `CLAUDE.md:73-82`.*
12. **Reword the "'not set up yet' UI state is retired" claim** to "defensive fallback, unreachable in normal operation". Do not attempt the deletion — six setters, a detector, and six gated branches. *Evidence: `index.html:3448`, `:3504-3566`, `:1009-1013`, `:3968-3974`, `:3976/3990/4000/4009/4028/4212`.*
13. **Regenerate the analytics event list in `CLAUDE.md:76-82`** mechanically from the `track()` call sites (28 names), or replace it with a pointer plus the naming convention so it can't drift again.
14. **Fix the four dead/stale doc citations:** `scratchpad/rls_audit.js` in `BACKLOG.md:74` and in `CLAUDE.md`; `BACKLOG.md:609`'s "15/15" (the suite is now 33/33); add a one-line Architecture note on `design-system/` (12 files, not deployed, authoritative or not).
15. **Sweep `BACKLOG.md` for the old host in forward-looking text** — `:1` title, `:130` "ScrubPay ingest endpoint", `:468` invite-link shape. Leave the Done log alone.
16. **Rewrite `README.md`'s Deployment section** (or delete it and point at `CLAUDE.md`): name `claude/migrate-to-github-deploy-3F5RD`, the 3-file publish list, and badgebudget.com; drop the main/master claim (:25, :40), the github.io URL (:30) and the Netlify line (:44).
17. **Wire CI:** add a pre-publish job running `node scripts/test_groom_seed.mjs` plus a Babel parse of the `index.html` babel block; fail the deploy on error. Also pin the four actions to SHAs and `.mcp.json` to an exact version instead of `@latest`.
18. **Add `supabase/migrations/000_core.sql`** capturing `user_data`/`feedback`/`events` schema, RLS policies, grants and the `page`/`user_agent` columns. Highest-value infra gap, but a real session's worth of work against a production DB with no staging.
19. **Commit the test harness** — the Playwright iPhone-13 rig, `te_swap_p2_algo.js`, `rls_audit.js` — under `tests/`, so the verification `CLAUDE.md` leans on is reproducible. Currently 0 of it exists in git.
20. **Merge or close PR #46 and PR #60.** #46 is 10 days old, self-contained, gate-safe copy — decide it in light of the fact that it is a workaround for a paid custom-auth-domain add-on. #60 is docs-only, now 3 files / 270 insertions.
21. **Check the nightly Routine's prompt for a pinned `Claude-Session:` URL** — seven consecutive nights emitted the identical `session_01GGWUJsjTww44sqB58U5Ahp`, so no nightly run is traceable to its own session. Also record the Routine's trigger id, schedule and owning account in the handoff (it has zero repo representation).
22. **Scope the rename as its own session.** 21 occurrences on 18 `index.html` lines (8 user-visible: `:7`, `:386`, `:456`, `:2283`, `:2510`, `:3627`, `:3630`, `:4278`) plus 12 mechanical lines under `design-system/` and 5 in `docs/`. Do not touch the storage keys, the `@scrubpay` UID scheme, or the `'scrubpay-swaps'` salt. Preserve `CLAUDE.md:126-130` as naming history. Note `:456` needs a hand edit (escaped `\'`) and `:386` is a 24px/weight-800 wordmark — check a longer name on a narrow iPhone viewport. Add the non-code item: set the app name + logo on the GCP OAuth consent screen.
23. **Dedup `keyOf`/`parseISO`** (`index.html:1905-1906` against `:525-526`) and the 6 inline `split('-').map(Number)` sites (:741, :765, :1902, :2121, :3039, :3048) — **as a dedup cleanup only, with no perf claim.** Memoization needs `useCallback` on `keyOf`, `statOf` (:1926), `onPick` (:2611) and `secRef` (:2614) plus stable object props; that is the dedicated session `CLAUDE.md` already defers, not a one-line prerequisite.

### Explicitly uncertain / disputed

- **The nightly loop's "cannot git push from a scheduled run" note** (`BACKLOG.md:263-273`, bullet 1) is stale-*looking* but **not disproven** — git cannot distinguish scheduled from interactive pushes. Re-verify from an actual scheduled run before deleting it. The sibling "no Management API" bullet *is* provably false and can go now.
- **The `Claude-Session:` trailer explanation is a hypothesis**, not a verified fact. Pinned-in-the-Routine-prompt is the most likely cause given CLAUDE.md says each fire is a fresh session, but nothing in the repo proves it.
- **All live-database assertions are prose-only from the repo's point of view** — RLS on `user_data`, insert-only RLS on `feedback`/`events`, "29/29 + 5/5" audit results, auth Site URL, "DB 11MB / 500MB". The one live check performed was the `'scrubpay-swaps'` salt in `public.swap_board`, which confirmed. Everything else is unverified.
- **Registrar/DNS/WHOIS facts are unverifiable from the repo.** Two in-repo sources corroborate each other (`docs/session-2026-09-02-domain-and-naming.md:20/:22/:98-107` and `CNAME` + `deploy.yml:42`) but both are the same author's prose. Record the actual registrar and DNS record values in the handoff; they exist nowhere in the repo.
- **`BACKLOG.md:298` (`swap-channel-manager-visibility`)** is *arguably* covered by the parked swap-handoff item's L196-197, but that is inference, not the item's own words. `:299` is definitely not covered.