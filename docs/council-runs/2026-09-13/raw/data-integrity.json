[
 {
  "slice": "wage-core",
  "findings": [
   {
    "title": "pagehide/visibilitychange flush silently drops an edit when the best-effort save fails",
    "severity": "high",
    "where": "index.html:2489-2505 (flush), contrast with the debounced save effect's failure handling at index.html:2470-2479; saveToSupabase at index.html:1324-1344",
    "detail": "The debounced save effect explicitly guards against a failed cloud write: when saveToSupabase(...) resolves null (it always resolves, never throws — see the catch at 1339-1343 and the error branch at 1337), the else branch at line 2478 calls writeBackup(user.id, dataToSave) so the edit survives locally under BACKUP_KEY. The separate `flush` handler registered for `pagehide`/`visibilitychange`→hidden (added specifically because '500ms debounce would otherwise drop it', per the comment at 2489) does not do this: its call chain is `saveToSupabase(...).then(ts=>{ if(ts){ ...clear backup... } })` — the `if(ts)` guard only handles the success path. When `ts` is null/falsy (network drop, RLS/Postgres error, or the fetch simply being torn down mid-flight as the page actually unloads), the `.then` body does nothing: no writeBackup call, no error surfaced.",
    "failure": "Signed-in user edits a shift, then immediately backgrounds or closes the tab (the debounce hasn't fired yet, which is exactly the case this handler exists for) on a flaky connection. The pagehide-triggered save fails; because flush's failure branch is a no-op, the edit is written to BACKUP_KEY nowhere, never reaches Supabase, and — once the tab is actually gone — no longer exists in memory either. On the next load, hydrateFromCloud finds no newer backup and no newer cloud row, so the edit is gone with no toast, no syncError, and no recovery path, unlike every other save-failure path in the app.",
    "fix": "Mirror the debounced effect's failure handling in flush: when saveToSupabase resolves falsy, call writeBackup(snap.user.id, snap.data) just like line 2478 does. Because a `.then()` callback isn't guaranteed to run once actual page teardown begins, prefer writing the backup synchronously before firing the best-effort save (optimistic backup, cleared only on confirmed success) rather than only in the async continuation.",
    "wageCore": false
   },
   {
    "title": "An open Add-Shift sheet's stale in-memory draft can silently overwrite a same-day edit just pulled from another device",
    "severity": "medium",
    "where": "AddShiftSheet's list/events/note state is captured once at mount from props (index.html:4076, 4083-4084) and never resynced; saveDayShifts unconditionally overwrites shifts[dateKey]/dayEvents[dateKey]/notes[dateKey] with that draft (index.html:2857-2874); the cross-device poll can apply a newer remote blob into the same state at any time the sheet is open (applyData at 2317/2325-2326 via the 15s interval, focus, and visibilitychange refetch at index.html:2547-2574)",
    "detail": "AddShiftSheet is only mounted/unmounted on `sheetDate` null↔value transitions (index.html:3613-3618: `{sheetDate && <AddShiftSheet .../>}`, no `key` prop keying it to the date), so while it stays open its `list`/`events`/`note` state — seeded once from `existing`/`existingEvents`/`existingNote` at mount — never re-reads the `shifts`/`dayEvents`/`notes` props even if they change underneath it. But those props DO change underneath it: the cross-device refetch (2547-2574) fires on the 15s interval and immediately on `focus`/visible-`visibilitychange`, and on a content difference calls `applyData(row.data,{keepPeriod:true})`, which does a plain `setShifts(d.shifts)`/`setDayEvents(d.dayEvents)`/`setNotes(d.notes)` (2317, 2325-2326) — a full replace, not a per-day merge. When the sheet is later saved or dismissed-with-dirty-state, `saveDayShifts` (2857-2858) does `n[dateKey]=arr` (or the dayEvents/notes equivalents at 2867/2873) against whatever `prev` currently is, but `arr`/`events`/`note` are still the sheet's original stale draft.",
    "failure": "Nurse signs into the same account on two devices. On device A she opens the Add-Shift sheet for a day and gets interrupted — she switches apps/tabs (backgrounding triggers no refetch, but returning fires an immediate `visibilitychange`/`focus` refetch). Meanwhile on device B she (or someone else with the login) added a shift to that same day; that write is now the newer cloud row. When she returns to device A, the refetch pulls device B's shift for that day into `shifts` — but the still-open sheet's `list` doesn't include it. She finishes her edit on device A and hits Save: `saveDayShifts` writes only her stale `list` for that date, so device B's just-synced shift for that day is silently discarded, then propagated to the cloud 500ms later with no conflict warning.",
    "fix": "When shifts/dayEvents/notes for the currently-open `dateKey` change while the sheet is mounted, either re-seed the sheet's local draft (e.g. a useEffect keyed on the incoming day's content that merges or warns instead of overwriting) or have the cross-device refetch skip applying changes to a day that has an open, dirty sheet and re-check once it closes.",
    "wageCore": false
   }
  ]
 },
 {
  "slice": "data-sync",
  "findings": [
   {
    "title": "15s cross-device refetch can silently discard an edit that hasn't finished its 500ms debounce/network round-trip",
    "severity": "high",
    "where": "index.html:2547-2574 (poll/refetch effect), specifically the guard at 2557 and the applyData call at 2561; interacts with the debounced save at 2453-2487 (500ms timer at 2485)",
    "detail": "The comment at index.html:2540-2543 claims the poll is 'guarded by updated_at AND content-equality so we never ... clobber unsaved local edits.' In fact `normalize()` (2550) only prevents re-applying byte-identical content (guards against echoing an already-synced row). It has no way to detect 'this device has a local edit that is still sitting in its 500ms debounce timer, or whose saveToSupabase call is still in flight, and hasn't reached the cloud yet.' `refetch()` only checks `hydratedForUser.current===user.id` (2553) before calling `applyData(row.data,{keepPeriod:true})` (2561) whenever `row.updatedAt` is newer than `lastSeenAt.current` and the fetched content differs from the current in-memory serialization — which it necessarily will differ whenever there is a pending, not-yet-saved local edit, since that edit by definition isn't reflected in either the fetched row or (yet) anywhere durable.",
    "failure": "Same account signed in on two devices/tabs, A and B, both hydrated and in sync (lastSeenAt equal). Device A edits something (e.g. adds a shift) and its save completes, advancing the cloud row's updated_at. Moments later Device B makes an unrelated edit (e.g. corrects the base rate) — B's own 500ms debounce timer starts, and the network round-trip to Supabase hasn't completed yet. If B's `setInterval(refetch,15000)` tick, or a `focus`/`visibilitychange` event (2568-2571), fires during that window, `refetch()` sees A's newer `updated_at`, sees the fetched content differs from B's current state (because it doesn't yet include B's edit, which never reached the cloud), and calls `applyData(row.data,{keepPeriod:true})` — replacing ALL of B's in-memory state, including the edit just made, with A's snapshot. There is no merge, no conflict warning, and no backup: `writeBackup` (2273-2274) is invoked only when a cloud save FAILS, never when a remote refetch overwrites in-memory state. B's edit is gone with no indication anything happened, and B's own still-pending debounced save (now reflecting the reverted state) will shortly re-persist that reverted state to the cloud.",
    "fix": "Before applying a remote row in `refetch()`, check whether a local save is currently pending or in flight (e.g. track a `saveInFlight`/`pendingLocalEdit` ref set when the debounce timer is armed or `saveToSupabase` is awaited, cleared once the write lands) and skip/defer the apply — or queue the refetched row and re-check content-equality once the pending local save resolves — instead of unconditionally overwriting state whenever content differs from the last-seen snapshot.",
    "wageCore": false
   }
  ]
 },
 {
  "slice": "calendar-sync",
  "findings": [
   {
    "title": "Stale .ics import plan can resurrect a shift another device deleted, or clobber a concurrent edit, and the result gets persisted to the cloud",
    "severity": "high",
    "where": "index.html:3147-3191 (icsConfirmImport), 3089-3105 (buildIcsImportPlan closes over live `shifts`/`dayEvents`), 1511-1543 (icsPlanFromExisting captures shiftType/bonusType/hours at plan-build time), 2547-2574 (15s cross-device poll calls applyData which does a full setShifts(d.shifts) overwrite)",
    "detail": "The .ics import/sync merge plan (`toUpdate`, with `prevShiftId`/`shiftType`/`bonusType`/`hours` frozen into `icsImport` state) is built once, from whatever `shifts`/`dayEvents` happened to be in scope at parse time (a manual file import, or `runIcalSync`'s up-to-12s proxy fetch). The user then reviews a multi-step questionnaire before tapping Confirm — this can take anywhere from seconds to a minute or more for several shift groups. During that whole window the independent cross-device sync poll (index.html:2547-2574) keeps running every 15s while the tab is visible, and on finding a newer cloud row calls `applyData(row.data,{keepPeriod:true})`, which does an unconditional `setShifts(d.shifts)` (index.html:2317) — a full state replacement, not a merge. When the user finally taps Confirm, `icsConfirmImport`'s `setShifts(prev=>...)` (3163) operates on whatever `prev` is *now* (post-poll), but for every `toUpdate` entry it unconditionally does `dropShift(u.prevDateKey,u.prevShiftId)` (no-op if that id is already gone) and then unconditionally re-inserts `{id:u.prevShiftId, shiftType:u.shiftType, hours:u.hours, ...}` (3171-3176) using the values captured at plan-build time — with no check that the shift still exists, is still at the state the plan assumed, or wasn't intentionally deleted/edited elsewhere in the meantime.",
    "failure": "Nurse has BadgeBudget open on her phone and signed in on a tablet too (the app's advertised two-device use case). On the phone she opens Settings and taps 'Sync now' (or the silent app-open auto-sync fires and finds something to confirm), which opens the import questionnaire with several shift groups to classify. While she's still working through the cards, she deletes a shift on the tablet (it got cancelled) and it saves to the cloud. Within 15s the phone's background poll pulls that newer blob and removes the shift from `shifts` there too. She finishes the questionnaire on the phone and taps Confirm: `icsConfirmImport`'s stale `toUpdate` entry for that same icsUid re-adds the shift she just deleted on the tablet, with its old id/date/hours/pay type. The debounced save effect (2453-2487) then pushes this reconstituted shift back to Supabase, overwriting — permanently, last-writer-wins — the deletion she made on the other device. The same mechanism also silently reverts a pay-type/hours edit made on the other device mid-sync, since the plan's `shiftType`/`bonusType`/`hours` were snapshotted before that edit landed.",
    "fix": "Re-derive (or at least re-validate) the merge plan against the current `shifts`/`dayEvents` at Confirm time rather than trusting the snapshot captured when the sheet opened — e.g. re-run `buildIcsImportPlan` (or just check that `u.prevShiftId` still exists at `u.prevDateKey` with the same fields) inside the `setShifts` functional updater, and drop/re-warn on any `toUpdate`/`toRemove` entry whose target shift already vanished or changed for a reason other than this sync.",
    "wageCore": false
   },
   {
    "title": "A shift that moved outside the 60-day-back/366-day-forward sync window is indistinguishable from a deleted shift, and the confirm screen states it as certain",
    "severity": "medium",
    "where": "index.html:1545-1576 (icsPlanFromExisting removal logic, esp. `if(!inWindow(rec.dateKey)) return;`), 826-864 (parseICSSchedule drops out-of-window events before the caller ever sees them), 3828-3834 (IcsImportSheet: \"Removing … — gone from your calendar feed.\")",
    "detail": "`parseICSSchedule` silently drops any VEVENT outside [today-60d, today+366d] (`droppedWindow`, 864) — the caller never learns the event still exists at a new date outside that range. `icsPlanFromExisting`'s removal pass then treats \"this icsUid wasn't seen in the fresh parse\" as \"deleted from the calendar\" as long as the *previously stored* dateKey is inside the window (1558-1566) — it has no way to tell \"deleted\" apart from \"moved to a date the current fetch window doesn't cover.\" The confirm sheet then asserts, unconditionally, that the listed shifts are \"gone from your calendar feed\" (3832-3834) with no hedge.",
    "failure": "A shift that was correctly imported near-term gets rescheduled in the source calendar to more than 366 days out (e.g. a hospital or travel-nurse contract that publishes far-future dates) or corrected to a date more than 60 days in the past. The next sync no longer sees that UID in-window, proposes it under \"Removing … gone from your calendar feed,\" and if the user (trusting the app's own confident wording) taps Confirm, `icsConfirmImport` permanently deletes a shift that was never actually cancelled — losing real logged hours/pay-type data with no undo.",
    "fix": "Either widen/soften the removal claim when the shift's own recorded date is near a window boundary, or track that the UID was seen previously and simply skip removal (leave it stored, unflagged) when its own current window straddles a boundary, and change the confirm copy from an assertion (\"gone from your calendar feed\") to a hedge (e.g. \"no longer appears in the synced window\").",
    "wageCore": false
   }
  ]
 },
 {
  "slice": "infra",
  "findings": [
   {
    "title": "Whole-blob upsert has no compare-and-swap: a delayed/flaky-network write can silently roll back a newer, already-\"saved\" edit from another device, with no backup to recover it",
    "severity": "high",
    "where": "saveToSupabase() index.html:1324-1344; sync poll ordering check index.html:2551-2564; MAX_BLOB_BYTES/backup guard index.html:2454-2487",
    "detail": "saveToSupabase() computes `updatedAt = new Date().toISOString()` on the CLIENT before the network call, then does `.upsert({user_id, data, updated_at}, {onConflict:'user_id'})` with no WHERE/version guard (confirmed: no trigger or check constraint on user_data.updated_at in supabase/migrations/000_core.sql — it's a plain `default now()` column, never conditioned on in the upsert). Whichever request physically reaches Postgres LAST wins outright and overwrites both `data` and `updated_at`, regardless of which edit happened later in real time or what timestamp value it carries. The client-side ordering guard at index.html:2557 (`row.updatedAt > lastSeenAt.current`) only protects the LOCAL in-memory comparison on the polling device; it does nothing to stop the DB row itself from being overwritten by an out-of-order or clock-skewed write from another device.",
    "failure": "Nurse edits a shift on her phone at a hospital with spotty WiFi (edit at real time T0); the save request is delayed in flight. Seconds later she edits the same data on her laptop at home over a good connection (edit at T1>T0); that save completes immediately and succeeds — clearing the laptop's own local backup (index.html:2475) because its save succeeded. If the phone's delayed request then arrives at Supabase after the laptop's, the row is overwritten with the phone's STALE data and an OLDER `updated_at`. The laptop's newer edits are now gone from the cloud. Because they were never in `writeBackup()` (that only fires when a save FAILS, and the laptop's save reported success), there is no copy anywhere to recover them once the laptop's tab is closed or its browser data cleared — reopening the app anywhere else (or signing in on a new device) hydrates the phone's older data. The laptop's own 15s poll never flags this either, because the phone's `updated_at` is chronologically earlier than the laptop's `lastSeenAt`, so `row.updatedAt > lastSeenAt.current` is false and the discrepancy is never even surfaced.",
    "fix": "Add an optimistic-concurrency guard to the upsert (e.g. pass the last-seen `updated_at` and use `.eq('updated_at', lastSeenAt)` with an update, falling back to insert/upsert only when no row exists yet), or move to a monotonic version column with a DB-side check so a stale write is rejected (surfacing a conflict to the user/backup path) instead of silently winning. This does not require full versioned ops (PR #67's scope) — just enough to stop a slower write from clobbering a faster one.",
    "wageCore": false
   },
   {
    "title": "differentials is the one user-growable collection in sanitizeData with no cap, so it alone can push the blob past MAX_BLOB_BYTES and freeze cloud sync with no cross-device recovery",
    "severity": "medium",
    "where": "sanitizeData differentials branch index.html:1041-1050 (no slice/cap, contrast with goals at 1072-1082 and MAX_GOALS at 634); addDiff() index.html:4692 (unconditional push, no length check unlike the goals-add path at 4685)",
    "detail": "Every other user-appendable collection is capped both in the UI and in sanitizeData: goals (MAX_GOALS=12, enforced at index.html:4685 and sliced at 1076), templates (MAX_TEMPLATES, sliced at 1058), patterns (MAX_PATTERNS=8, sliced at 1093), dayEvents (MAX_DAY_EVENTS per day, sliced at 1006). `differentials` has neither: `addDiff()` at 4692 unconditionally adds a new `custom-<timestamp>` key with no length check, and sanitizeData's differentials block (1041-1050) validates each entry's shape but never slices or caps the count.",
    "failure": "A user who keeps adding custom differentials (e.g. one per unit/role variant she works) eventually pushes the serialized blob past MAX_BLOB_BYTES (512KB). At that point the save effect's guard at index.html:2464-2469 returns BEFORE calling saveToSupabase at all — the cloud row freezes at whatever it held before the last differential that tipped it over, and the toast fires once (`oversizeWarned.current`) and is easy to dismiss/miss. If she then signs in on a second device, hydrateFromCloud pulls that frozen, now-stale cloud row: the differentials she added after crossing the cap are missing there, with the only copy sitting in the first device's `writeBackup()` local mirror.",
    "fix": "Add a MAX_DIFFERENTIALS cap enforced both in addDiff() and as a `.slice(0, MAX_DIFFERENTIALS)` in sanitizeData's differentials branch, matching the pattern already used for goals/templates/patterns/dayEvents.",
    "wageCore": false
   }
  ]
 }
]
