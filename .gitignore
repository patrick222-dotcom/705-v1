# dependencies / vendored harness packages
node_modules/
vendor/

# secrets — tokens live in the environment, never in the tree
.env
.env.*
*.pem
*.key
.claude/settings.local.json

# session scratch. The Playwright harness now lives in tests/ and is reproducible; this is
# the copy of index.html it rewrites to local script paths at run time.
scratchpad/
_site/
.harness-scratch/

# test output
test-results/
playwright-report/
*.log

# OS / editor
.DS_Store
Thumbs.db
